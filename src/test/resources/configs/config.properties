# Config Properties to set browser
browserType = edge


# Maven Properties
# threadCount is the amount scenarios that will be ran in parallel
# e.g. 2 threads will open 2 drivers which will run through their own respective scenarios
threadCount=2
